# Foundry build artifacts
cache/
out/

# Keep broadcast/ TRACKED as the on-chain audit trail of deployments,
# but ignore the local-only dry-run subdirectories.
broadcast/**/dry-run/

# Forge deps managed by `forge install`
# (uncomment if treating lib/ as vendored)
# lib/

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Coverage
lcov.info
coverage/

# Deployments (keep committed; uncomment if managed off-repo)
# deployments/

# test/Deployments.t.sol writes to these chainIds during the suite — never commit them
deployments/3133700*.json
