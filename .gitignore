node_modules
.env
coverage
coverage.json
typechain
typechain-types

# Hardhat files
cache
artifacts


# LayerZero specific files
.layerzero

# foundry test compilation files
out

# yarn 4 (Berry) — yarn binary is provisioned by corepack via the
# `packageManager` field in package.json, so nothing under .yarn/ is
# committed. .pnp.* is unused (we run with nodeLinker: node-modules).
.yarn
.pnp.*
yarn-debug.log
yarn-error.log

# Editor and OS files
.DS_Store
.idea
